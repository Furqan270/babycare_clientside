import React from 'react';

const UserNav = () => {
return (
    <div className="flex flex-col userNav">
        <ul className="flex flex-col gap-4">
            <button>logout</button>
        </ul>
    </div>
)
}

export default UserNav;